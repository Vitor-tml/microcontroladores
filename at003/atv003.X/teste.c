int main()
{
    int altura = 8;
    int maximo, aux, altura, vezes;
    // ZERAFLAG
    do
    {
        maximo = altura - vezes;
        for(aux = maximo; aux > 0; aux --)
        for(aux = 0; aux < maximo; aux++)
        vezes++;
    }while(maximo);

}